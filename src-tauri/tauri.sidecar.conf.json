{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "bun run build && bun run build:sidecar"
  },
  "bundle": {
    "externalBin": ["binaries/docsreader-mcp"],
    "createUpdaterArtifacts": true
  }
}
